@model IEnumerable<TravPense.Data.Model.Activityy>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ActivityName)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ActivityName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location.LocationId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ActivityyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ActivityyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ActivityyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
